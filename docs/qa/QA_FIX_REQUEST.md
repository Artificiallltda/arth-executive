# QA Fix Request: arth-executive

**Generated:** 2026-03-10T03:15:00Z
**Target Module:** arth-executive
**Reviewer:** Orion (Master Orchestrator)

---

## Instructions for @dev

Fix the systemic issues identified during the hybrid architecture migration. Focus on stability, delivery sequence, and aesthetic quality.

---

## Summary

| Severity | Count | Status                  |
| -------- | ----- | ----------------------- |
| CRITICAL | 1     | Must fix before merge   |
| MAJOR    | 2     | Should fix before merge |
| MINOR    | 3     | Optional improvements   |

---

## Issues to Fix

### 1. [CRITICAL] API Retry Loops & System Latency (Gemini 429)

**Issue ID:** FIX-ARTH-001

**Location:** `src/core/graph.py` / Gemini SDK Configuration

**Problem:**
When Google Gemini hits rate limits (429), the system enters a 25-second silent retry loop, causing delivery times to exceed 8 minutes for simple images.

**Expected:**
The system should fail fast or switch to a stable model (OpenAI/DeepSeek) instead of retrying indefinitely. Remove `gemini_fallback` from the main logic nodes to prevent blocking.

**Status:** [x] Fixed (Removed fallback, system now fails gracefully or uses DeepSeek for text)

---

### 2. [MAJOR] PDF Content Overflow

**Issue ID:** FIX-ARTH-002

**Location:** `src/tools/doc_generator.py`

**Problem:**
Text in generated PDFs overflows the right margin when using calculated widths, making documents unprofessional and unreadable.

**Expected:**
Use `multi_cell(0, ...)` in FPDF to ensure automatic wrapping at the right margin.

**Status:** [x] Fixed (Width set to 0 for all text blocks)

---

### 3. [MAJOR] Incorrect Delivery Sequence (File before Text)

**Issue ID:** FIX-ARTH-003

**Location:** `src/router/message_handler.py`

**Problem:**
Files are delivered to Telegram before the AI explanation message, confusing the user.

**Expected:**
Explanation text must be delivered FIRST, followed immediately by the physical files.

**Status:** [x] Fixed (Logic inverted in `execute_brain` to send text then files)

---

### 4. [MINOR] Premium Design System Implementation

**Issue ID:** FIX-ARTH-004

**Location:** `src/tools/pptx_generator.py`, `src/tools/excel_tools.py`, `src/tools/doc_generator.py`

**Problem:**
Documents (DOCX, PPTX, Excel) are being generated with default/basic styling, lacking the "Executive Gold" aesthetic (Navy Blue, Calibri fonts, structured headers).

**Expected:**
Apply Manus AI Design System:
- **DOCX:** Calibri fonts, corporate blue titles.
- **Excel:** Navy headers with white text, auto-fit columns.
- **PPTX:** Dark Navy background, image auto-placement from tags.

**Status:** [x] Fixed (Styling injected into all generator tools)

---

### 5. [MINOR] Image Generation Redundancy (Multi-file delivery)

**Issue ID:** FIX-ARTH-005

**Location:** `src/agents/executor.md`, `src/core/graph.py`

**Problem:**
The system generates 4+ images (JPG+PNG mix) when a single image is requested, wasting tokens and confusing the user.

**Expected:**
Hard limit of 1 image per request unless a pair is explicitly requested. Force `FINISH` node immediately after image generation.

**Status:** [x] Fixed (Strict limits in persona and graph traps)

---

### 6. [MINOR] ETA Synchronization

**Issue ID:** FIX-ARTH-006

**Location:** `src/router/message_handler.py`

**Problem:**
"Analizando dados..." and other status messages arrive after the task is already in progress or completed.

**Expected:**
Trigger ETA callbacks immediately upon entering the corresponding node in the graph.

**Status:** [x] Fixed (Callbacks moved to the start of node execution)

---

## After Fixing

1. Mark each issue as fixed in this document.
2. Run `test_squad_v1.py` to verify full loop.
3. Request final user verification in Telegram.

---

_Generated by Orion (Master Orchestrator) - AIOS Stability System_
